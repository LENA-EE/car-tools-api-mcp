services:
  - type: web
    name: car-tools-api
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: OPENROUTER_API_KEY
        sync: false
      - key: NODE_ENV
        value: production
      - key: FNP_MOCK
        value: "true"
    healthCheckPath: /health
